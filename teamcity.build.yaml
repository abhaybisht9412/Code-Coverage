version: "2023.05"

steps:
  - name: Install dependencies
    script:
      - npm install

  - name: Run tests with coverage
    script:
      - npm run test:coverage

  - name: Report coverage to TeamCity
    script:
      - echo "##teamcity[importData type='lcov' tool='jest' path='coverage/lcov.info']"